'use strict';

var app = angular.module("app",[]); 
/*
var app = angular.module("app")
                  .congif(['$httpProvider', function($httpProvider){
						$httpProvider.defaults.useXDomain = true;
						delete $httpProvider.defaults.header.common['X-Requested-With']; 
					}]); */

